"""Workflow internals - low-level orchestration primitives.

Houses the ``Pipeline`` engine, ``Step`` protocol, ``PipelineState``
hierarchy, the DuckDB ``StepsLedger``, the ``CheckpointStore`` and the
``DerivedRegistry``. Public re-exports are pinned in S02-04.
"""
