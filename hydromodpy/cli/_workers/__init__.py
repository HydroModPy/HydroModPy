"""Private helpers consumed by hydromodpy.cli.commands.<family>.<action>.run.

Functions live here instead of `hydromodpy._api` so the public ``hmp.*``
namespace stays focused on the 13 user-facing verbs.
"""
