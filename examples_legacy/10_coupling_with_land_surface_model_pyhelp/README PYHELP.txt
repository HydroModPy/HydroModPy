Before using PyHelp, make sure to install the provided Conda environment on your computer.
You can find the environment file (.yml) in the "install" folder of the Git repository.

cd /d "path/where/is/the/install/directory/"
conda env create -f env_pyhelp-0.1_windows.yml -n pyhelp-0.1
