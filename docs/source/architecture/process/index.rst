Process Architecture
====================

This section documents the process runtime layer implemented in
``hydromodpy.physics``: the package map, the reusable contracts, the
runtime data model, and every UML view (component, class, sequence,
lifecycle, extension).

For solver-side meshing and structured-grid documentation, see
:doc:`../mesh/index`.

.. toctree::
   :maxdepth: 2

   process-architecture
