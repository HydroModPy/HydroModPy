# .readthedocs.yaml
# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details

# Required
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  apt_packages:
    - graphviz
    - pandoc
    - plantuml
  jobs:
    install:
      - python -m pip install --upgrade --no-cache-dir pip setuptools wheel
      - python -m pip install --exists-action=w --no-cache-dir -r docs/readthedocs_requirements.txt
      - python -m pip install --no-build-isolation --no-deps --no-cache-dir .
    build:
      html:
        - python -m sphinx -T -j auto -b html -d _build/doctrees -D language=en docs/source $READTHEDOCS_OUTPUT/html

sphinx:
  configuration: docs/source/conf.py
