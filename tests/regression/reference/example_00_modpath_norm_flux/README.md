This dataset is a frozen reference case derived from the Example 00 workflow.

Purpose:
- provide deterministic binary outputs for non-regression tests;
- validate `.npy` outputs from MODFLOW post-processing;
- validate `.npy` snapshots generated by MODPATH post-processing and filtering.

Main entry point for tests:
- `results_simulations/reg_0`

Notes:
- files are intentionally versioned as test fixtures;
- update this dataset only when expected scientific outputs are intentionally changed.
- golden signatures used by pytest are stored in:
  `tests/regression/reference/golden_references/example_00_npy_signatures.json`.
