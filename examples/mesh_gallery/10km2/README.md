# 10 km2 Cases

Reserved for versioned mesh-gallery cases at the `10 km2` target scale.

Recommended inventory for the first teaching pair:

- one `geology_rivers_buffer30` case
- one `rivers_only_buffer30` case

Use `python -m tools.doc_gallery.import_mesh_bundle ... --scale 10km2 ...`
to import one local bundle here.
