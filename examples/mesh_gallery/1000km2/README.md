# 1000 km2 Cases

Reserved for versioned mesh-gallery cases at the `1000 km2` target scale.

Recommended inventory for the first teaching pair:

- one `geology_rivers_buffer30` case
- one `rivers_only_buffer30` case

These bundles are typically much larger than the `100 km2` ones, so keep the
selection deliberately small and pedagogically distinct.
