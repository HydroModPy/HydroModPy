# Generated truth package

This directory is reserved for generated B0 pseudo-observations.

Expected files:

```text
metadata.json
normalization.json
steady_network_drain_by_cell.npz
steady_network_active_mask.npz
transient_q_total_release.csv
cell_geometry.npz
```

The package should be regenerated from the truth configurations rather than
edited by hand.

