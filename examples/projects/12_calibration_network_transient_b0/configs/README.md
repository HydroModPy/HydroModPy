# B0 configuration stubs

These TOML files are contract stubs for the standalone B0 prototype. They are
not yet wired to a launcher. Keep them small until the MODFLOW 6 extractor and
truth package generator are implemented.

