Assets for the recharge-ramp cross-code benchmark.

- `ramp_reference_k.*`: reference conductivity run with the committed six-method benchmark
- `ramp_high_k.*`: high-conductivity run with the committed four-method benchmark

Both files are regenerated by `tools/doc_gallery/generate_code_comparison_assets.py`.
