# PETSc `flow/boussinesq` validation runtimes read `[flow]` defaults from this
# file before applying case-specific values from `runtime_boussinesq.py`.

[flow]
runtime_backend = "petsc"
surface_interaction_model = "ts_vi_obstacle"
