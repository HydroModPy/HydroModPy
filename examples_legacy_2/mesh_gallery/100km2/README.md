# 100 km2 Cases

Reserved for versioned mesh-gallery cases at the `100 km2` target scale.

Recommended inventory for the first teaching pair:

- one `geology_rivers_buffer30` case
- one `rivers_only_buffer30` case

If several `100 km2` families coexist (`headwater`, `s3`, etc.), prefer a slug
and title that encode that family explicitly.
